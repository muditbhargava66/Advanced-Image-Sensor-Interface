# Core dependencies for basic functionality (matches pyproject.toml).
# For full optional features, install via: pip install -e ".[full]"
# For development dependencies, install via: pip install -e ".[dev]"

numpy>=1.23.5
scipy>=1.10.0
matplotlib>=3.7.0
Pillow>=12.3.0,<13.0.0
tqdm>=4.66.0
psutil>=5.9.0
